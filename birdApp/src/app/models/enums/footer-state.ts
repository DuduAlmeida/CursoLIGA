/*** 
 * O enum que representa os tipos dos botões do footer
 */
export enum FooterState {
    
    /*** 
     * Menu categorias selecionado
     */
    CATEGORIES = 0,
    /*** 
     * Menu todos comentários selecionado
     */
    ALL_COMMENTS = 1,
    /*** 
     * Menu meus comentários selecionado
     */
    MY_COMMENTS = 2,
}