/*** 
 * A interface que contém as características de um avatar
 */
export interface AvatarItem{
    
    /*** 
     * O emoji que representa o avatar
     */
    personEmoji: string;

    /*** 
     * A cor que representa o avatar
     */
    personColor: string;
}